{
	"id": "python.data.repository",
	"name": "Python Repository",
	"description": "Repository with simple CRUD contract",
	"category": "file",
	"subcategory": "data",
	"tags": ["python", "repository", "crud", "data"],
	"framework": "none",
	"language": "python",
	"kind": "file",
	"multiFile": false,
	"type": "repository",
	"output": {
		"fileName": "{{fileNameSnakeCase}}_repository.py",
		"folder": ""
	},
	"priority": 85,
	"complexity": "medium",
	"usage": "common",
	"recommended": true,
	"template": [
		"\"\"\"{{fileNamePascalCase}}Repository",
		"",
		"Repository abstraction for {{fileNameSnakeCase}} persistence.",
		"Generated on {{date}} by {{author}}",
		"\"\"\"",
		"",
		"from typing import Any",
		"",
		"",
		"class {{fileNamePascalCase}}Repository:",
		"    \"\"\"Data access for {{fileNameSnakeCase}} entities.\"\"\"",
		"",
		"    def create(self, payload: dict[str, Any]) -> dict[str, Any]:",
		"        \"\"\"Create and return a new entity.\"\"\"",
		"        if not payload:",
		"            raise ValueError('payload must not be empty')",
		"",
		"        return {'id': 1, **payload}",
		"",
		"    def get_by_id(self, item_id: int) -> dict[str, Any] | None:",
		"        \"\"\"Get one entity by id.\"\"\"",
		"        return {'id': item_id}",
		"",
		"    def update(self, item_id: int, payload: dict[str, Any]) -> dict[str, Any]:",
		"        \"\"\"Update an entity and return the new state.\"\"\"",
		"        if not payload:",
		"            raise ValueError('payload must not be empty')",
		"",
		"        return {'id': item_id, **payload}",
		"",
		"    def delete(self, item_id: int) -> bool:",
		"        \"\"\"Delete an entity by id.\"\"\"",
		"        return True"
	]
}
